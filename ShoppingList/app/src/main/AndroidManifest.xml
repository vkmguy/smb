<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pjait.shopping">

    <permission
        android:name="shopping.sp.ADD_ITEM_PERM"
        android:label="add_item_perm"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">
        <activity
            android:name=".ui.shoppingList.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.shoppingList.OptionsActivity"
            android:exported="true" />
        <activity
            android:name=".ui.shoppingList.ProductListActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>
